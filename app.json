{
  "name": "PRBot",
  "description": "A small bot for managing code review process on Github",
  "env": {
    "BUILDPACK_URL": "https://github.com/heroku/heroku-buildpack-go",
    "SLACK_TOKEN": {
      "description": "Access token for a Slack user that can post messages",
      "required": true
    },
    "SLACK_HOOK": {
      "description": "The URL of an 'incoming webhook' in Slack.",
      "required": true
    }
  },
  "addons": [
    "papertrail:fixa"
  ]
}

